{
  "name": "qr-code-generator",
  "version": "1.0.0",
  "description": "QR code generator that uses npm package inquirer and qr-image",
  "main": "index.js",

  "_commentTYPE": "By default `type` is set to `commonJS`. So in-order to use `import` change `type` to `module`",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kiku",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^9.2.7",
    "qr-image": "^3.2.0"
  }
}
